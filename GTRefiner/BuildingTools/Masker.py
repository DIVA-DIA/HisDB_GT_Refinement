

class Masker:
    """ Given a binary layer class: `Layer` and an Image class `PIL.Image` in RGB mode, keep all pixels that have
    1 in the binary layer and set the rest to black (0,0,0).
    Both layers must have the same dimensions.
    """
    pass