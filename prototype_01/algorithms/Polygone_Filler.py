# Dieses script soll jedes Polygon einer Seite mit einer Farbe Füllen.

# option 1
# int within
# für jedes pixel auf der Seite (von links nach rechts, zeile nach zeile)
#   wenn es sich um ein polygon-pixel handelt
#       testen, ob es sich um ein top oder bottom pixel handelt
#           falls es eines ist -> ignorieren
#           falls es keines ist -> within += 1
#               if within % 2 == 1:
#                   mark the current cell as being in polygon

# option 2
# or simply use InmageDraw

def fill_polygone(polygon):
    pass


